name: CI/CD

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: No stack detected
        run: echo "Aucune stack connue détectée automatiquement. Adaptez ce pipeline à votre projet."
